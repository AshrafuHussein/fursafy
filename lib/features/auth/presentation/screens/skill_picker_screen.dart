import 'package:flutter/material.dart';

/// S06 — Skill picker / profile setup screen. Placeholder.
class SkillPickerScreen extends StatelessWidget {
  const SkillPickerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Select Skills')),
      body: const Center(child: Text('Skill Picker Screen\nPlaceholder')),
    );
  }
}
